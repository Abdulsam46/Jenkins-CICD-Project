apiVersion: v1
kind: Service
metadata:
  name: spring-boot-dashboard-svc
  labels:
    app: spring-boot-dashboard
spec:
  type: NodePort
  selector:
    app: spring-boot-dashboard
  ports:
    - name: http
      port: 80
      targetPort: 8080
      nodePort: 30080
